# Project Notes

## Local Port Assignments

- Use web port `3300` for Etsy-Image development.
- The packaged/standalone desktop server keeps using `38477`.
- Use `3927` only for the temporary Codex brainstorming visual companion.
- Do not move this project back to the shared default `3000` unless the user explicitly asks.
